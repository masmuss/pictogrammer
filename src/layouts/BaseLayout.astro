---
import { ClientRouter, fade } from "astro:transitions";
import BaseHead from "@/components/layout/BaseHead.astro";
import Footer from "@/components/layout/Footer.astro";
import Header from "@/components/layout/Header.astro";
import ThemeProvider from "@/components/ui/ThemeProvider.astro";

const { ...head } = Astro.props;
---

<!doctype html>
<html lang="en">
	<head>
		<BaseHead {...head} />
		<ThemeProvider />
		<ClientRouter />
		<script src="/src/assets/scripts/mermaid-theme-fix.js" is:inline></script>
	</head>
	<body
		class="bg-background flex flex-col justify-center antialiased transition-all"
	>
		<main
			class="relative z-10 mx-auto flex min-h-screen w-full max-w-2xl flex-col items-center px-5 leading-relaxed md:max-w-5xl md:px-10 lg:px-0"
			transition:animate={fade({ duration: 300 })}
		>
			<div id="progress"></div>
			<Header />
			<div class="w-full flex-1">
				<slot />
			</div>
		</main>
		<Footer />
	</body>
</html>
