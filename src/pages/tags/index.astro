---
export const prerender = true;

import PageHeader from "@/components/ui/PageHeader.astro";
import BaseLayout from "@/layouts/BaseLayout.astro";
import { getAllPosts, getUniqueTagsWithCount } from "@/lib/contents";

const allPosts = await getAllPosts();
const sortedTags = getUniqueTagsWithCount(allPosts);
---

<BaseLayout
	title="All Blog Tags and Topics"
	description="Browse every topic tag used across the blog to quickly find posts about Astro, Go, software engineering, personal growth, and reflective writing themes."
	image={{ src: "/og/tags.png", alt: "All Tags - pictogrammmer" }}
>
	<div class="flex w-full flex-col gap-y-8">
		<PageHeader
			title="Tags"
			description="A list of all the topics I've written about in my posts."
			as="h1"
			class="mb-6"
		/>

		<div class="flex max-w-xl flex-col">
			<p class="text-muted-foreground">
				{sortedTags.length} tags across {allPosts.length} posts
			</p>
		</div>

		{
			sortedTags.length === 0 && (
				<p class="text-muted-foreground">No posts yet.</p>
			)
		}

		{
			sortedTags.length > 0 && (
				<div class="flex flex-wrap gap-3">
					{sortedTags.map(([tag, count]) => (
						<a
							href={`/tags/${tag}`}
							title={`View ${count} post${count > 1 ? "s" : ""} tagged with "${tag}"`}
							data-astro-prefetch
							class="group bg-secondary hover:bg-secondary/70 text-foreground hover:text-foreground flex items-center gap-2 rounded-lg px-4 py-2 transition-colors focus-visible:ring-2 focus-visible:ring-ring focus-visible:outline-none focus-visible:ring-offset-2"
						>
							<span class="font-medium">#{tag}</span>
							<span class="bg-muted text-muted-foreground group-hover:bg-primary group-hover:text-primary-foreground rounded-full px-2 py-0.5 text-sm transition-colors">
								{count}
							</span>
						</a>
					))}
				</div>
			)
		}
	</div>
</BaseLayout>
