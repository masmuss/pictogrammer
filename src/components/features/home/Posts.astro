---
import PostPreview from "@/components/features/blog/PostPreview.astro";
import type { CollectionPosts } from "@/types";

interface Props {
	posts: CollectionPosts[];
}

const { posts }: Props = Astro.props;
---

{
	posts.length > 0 && (
		<Fragment>
			<ul class="flex flex-col">
				{posts.map((p) => (
					<PostPreview post={p} showDescription />
				))}
			</ul>
			<div class="flex w-full items-end justify-end">
				<a href="/blog" class="flex items-center gap-x-2 text-sm">
					<span>See all posts</span>
					<span class="iconify ri--arrow-right-long-line" />
				</a>
			</div>
		</Fragment>
	)
}
